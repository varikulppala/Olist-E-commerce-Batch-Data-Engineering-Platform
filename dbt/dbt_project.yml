name: "olist_aws_batch_data_platform_adhi_2026"
version: "1.0.0"
config-version: 2

profile: "olist_aws_batch_data_platform_adhi_2026"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  olist_aws_batch_data_platform_adhi_2026:
    staging:
      +materialized: view

    intermediate:
      +materialized: view

    marts:
      +materialized: table

      dimensions:
        +materialized: table

      facts:
        +materialized: table
